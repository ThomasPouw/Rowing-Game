using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PeddalDetectors : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 normal = new Vector3();
        //Vector3 point = JustPtrck.Shaders.Water.WaveManager.instance.GetDisplacementFromGPU(transform.position, ref normal);
        //Debug.DrawLine(transform.position, point);
        //Debug.Log(transform.parent.name+ " Distnace: "+ Vector3.Distance(transform.position, point));
        //Debug.Log(transform.parent.name+ " transform Y "+ transform.position.y+ " point Y: "+ point.y);
    }
}
